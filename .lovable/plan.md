

# Plan: Renombrar la app a AxonTur

## Cambios

| Archivo | Cambio |
|---------|--------|
| `index.html` | Cambiar `<title>` y meta tags de "Lovable App" a "AxonTur" |
| `src/components/layout/Header.tsx` | Fallback de `'Generador de Presupuestos'` → `'AxonTur'`, eliminar subtítulo "Generador de Presupuestos" |
| `src/pages/Dashboard.tsx` | Título `h1` → "AxonTur", eliminar el `<p>` con "Crea presupuestos de viaje profesionales en minutos" |

4 archivos, cambios menores de texto/branding.

